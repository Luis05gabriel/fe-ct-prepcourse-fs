function resta(x, y) {
  // La función recibe dos argumentos "x" e "y" que son números.
  // Retorna el resultado de la resta.
  // Tu código:

  return x-y;
}

module.exports = resta;

console.log (resta(10,5));
console.log( "El resultado de la resta es : ",resta(25,25));