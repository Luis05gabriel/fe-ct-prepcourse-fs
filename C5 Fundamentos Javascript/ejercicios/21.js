function suma(x, y) {
  // La función recibe dos argumentos "x" e "y" que son números.
  // Retorna el resultado de su suma.
  // Tu código:

  return x+y;
}

module.exports = suma;


console.log (suma(22,24));
console.log ("la suma es : ", suma(2,2));